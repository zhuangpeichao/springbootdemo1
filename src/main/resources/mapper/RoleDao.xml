<?xml version="1.0" encoding="UTF-8" ?>
<!--id和接口中的方法名一一对应,必须唯一-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bdqn.springbootdemo.dao.RoleDao">
    <select id="querAll" resultType="Role">
        select * from tbl_role
    </select>
</mapper>